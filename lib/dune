(rule
 (targets config.ml)
 (action (run ./include/discover.exe)))

(library
 (name iomux)
 (public_name iomux)
 (modules iomux config poll util)
 (libraries unix)
 (foreign_stubs
  (language c)
  (names iomux_stubs)))
